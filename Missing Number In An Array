class Solution{
  public:
    int missingNumber(vector<int>& array, int n) {
        int sum=n*(n+1)/2,a=0;
        
        for(int i=0;i<n-1;i++)
        {
            a+=array[i];
        }
    
       return sum-a; 
    }
};
