class Solution {
  public:
    bool searchKey(int n, Node* head, int key) {
        for(int i=0;i<n;i++)
        {
            if(head->data==key) return 1;
            else
                head=head->next;
        }
        return 0;
    }
};
